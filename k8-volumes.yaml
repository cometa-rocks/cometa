apiVersion: v1
kind: PersistentVolume
metadata:
  name: cometa-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 3Gi
  accessModes:
    - ReadWriteMany
  hostPath:
        path: /cometa
        type: Directory
 